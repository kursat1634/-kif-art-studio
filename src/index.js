const SYSTEM = `Sen KIF ART STUDIO'nun web sitesindeki KIF AI tasarım ve proje asistanısın. Türkçe konuş. KIF ART STUDIO; özel metal tasarım, aydınlatma, mobilya, raf, tabela, dekoratif objeler ve mekân çözümleri tasarlar ve üretir. Tasarım fikri geliştirme, doğru soruları sorma ve proje brief'i toplama konusunda yardımcı ol. Bilmediğin teknik detay, stok, kesin fiyat veya teslim tarihi uydurma. Fiyatın; ölçü, malzeme, yüzey, adet, aydınlatma ve uygulamaya göre teklif ile netleşeceğini belirt. Kullanıcı teklif istiyorsa şu bilgileri toplamaya çalış: proje türü, kullanım alanı, yaklaşık ölçü, adet, malzeme/renk, aydınlatma ihtiyacı, şehir ve hedef tarih. Gerekirse hello@kifartstudio.com adresine yönlendir. Cevapları kısa, profesyonel, sıcak ve yaratıcı tut. KIF'in olmayan bir hizmeti kesin varmış gibi söyleme.`;
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"Content-Type","Access-Control-Allow-Methods":"POST,OPTIONS"};
export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/ai'){if(request.method==='OPTIONS')return new Response(null,{headers:cors});if(request.method!=='POST')return new Response(JSON.stringify({error:'Method not allowed'}),{status:405,headers:{...cors,'Content-Type':'application/json'}});try{const body=await request.json();const message=String(body.message||'').trim().slice(0,2000);if(!message)return new Response(JSON.stringify({answer:'Projenizi birkaç cümleyle anlatın; ölçü, kullanım alanı veya istediğiniz tarzdan başlayabiliriz.'}),{headers:{...cors,'Content-Type':'application/json'}});const result=await env.AI.run('@cf/openai/gpt-oss-120b',{messages:[{role:'system',content:SYSTEM},{role:'user',content:message}],max_tokens:450,temperature:.55});const answer=result?.choices?.[0]?.message?.content||result?.response||'Projenizi biraz daha açabilir misiniz?';return new Response(JSON.stringify({answer}),{headers:{...cors,'Content-Type':'application/json'}})}catch(error){return new Response(JSON.stringify({answer:'Şu an AI bağlantısında kısa bir sorun var. Projenizi hello@kifartstudio.com adresine gönderebilirsiniz.',error:'ai_error'}),{status:200,headers:{...cors,'Content-Type':'application/json'}})}}return env.ASSETS.fetch(request)}};
